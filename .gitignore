# Compiled binary
mgnt-server

# Runtime data (credentials, TLS certs, server registry)
data/

# Legacy config (replaced by data/.env)
config.json

# Any stray env files
.env
*.env

# Go build cache
*.test
*.out

# Agent binaries (built by Dockerfile, not committed)
public/downloads/agent-*

# Editor
.vscode/
.idea/
*.swp
*.swo
